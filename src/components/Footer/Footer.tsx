import React, { FC } from "react";

import * as Styled from "./Footer.styles";

export interface FooterProps {}

const Footer: FC<FooterProps> = () => {
  return <Styled.FooterWrapper></Styled.FooterWrapper>;
};

export default Footer;
